"use client"; import Link from "next/link"; import {useState} from "react"; import {useRouter} from "next/navigation"; import {Sparkles,ArrowRight} from "lucide-react"; import {call} from "../lib/api";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const x=await call("/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("access_token",x.access_token);localStorage.setItem("refresh_token",x.refresh_token);router.push("/dashboard")}catch(e){setError(e instanceof Error?e.message:"Erro")}finally{setBusy(false)}}return <main className="grid min-h-screen md:grid-cols-2"><section className="grid-bg hidden border-r border-line p-12 md:flex md:flex-col md:justify-between"><Link href="/" className="flex items-center gap-2 text-xl font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-lime text-ink"><Sparkles size={19}/></span>Nexora</Link><div><p className="max-w-lg text-4xl font-semibold tracking-tight">“A IA deixou de ser uma ferramenta e virou parte da nossa equipe.”</p><p className="mt-6 text-zinc-500">Times modernos trabalham na Nexora.</p></div><p className="text-sm text-zinc-600">Ambiente seguro e isolado por empresa</p></section><section className="flex items-center justify-center p-6"><form onSubmit={submit} className="w-full max-w-sm"><p className="text-sm text-lime">BEM-VINDO DE VOLTA</p><h1 className="mt-2 text-4xl font-semibold">Entre no workspace</h1><p className="mt-3 text-zinc-500">Use seu e-mail corporativo para continuar.</p>{error&&<div className="mt-6 rounded-xl border border-red-900 bg-red-950/40 p-3 text-sm text-red-300">{error}</div>}<label className="mt-8 block text-sm">E-mail<input className="mt-2 w-full rounded-xl border border-line bg-panel px-4 py-3 outline-none focus:border-lime" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="mt-5 block text-sm">Senha<input className="mt-2 w-full rounded-xl border border-line bg-panel px-4 py-3 outline-none focus:border-lime" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy} className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-lime py-3 font-semibold text-ink disabled:opacity-60">{busy?"Entrando...":"Entrar"}<ArrowRight size={17}/></button><p className="mt-6 text-center text-sm text-zinc-500">Ainda não tem uma empresa? <Link className="text-white" href="/cadastro">Criar conta</Link></p></form></section></main>}

